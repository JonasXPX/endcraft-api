spring:
  jpa:
    show-sql: true
  datasource:
    username: endcraft
    password: hackme
    url: jdbc:mysql://${DATABASE_HOST:localhost}:3306/endcraft?serverTimeZone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
